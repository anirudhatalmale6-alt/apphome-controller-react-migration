/**
 * Login Page
 * Route-level composition for login
 * Migrated from index.html login section
 */
import { LoginView } from '../features/authentication';

export const LoginPage: React.FC = () => {
  return <LoginView />;
};

export default LoginPage;
