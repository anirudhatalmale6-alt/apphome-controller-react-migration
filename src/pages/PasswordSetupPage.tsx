/**
 * Password Setup Page
 * Route-level composition for new user password setup
 */
import { PasswordSetupView } from '../features/authentication';

export const PasswordSetupPage: React.FC = () => {
  return <PasswordSetupView />;
};

export default PasswordSetupPage;
